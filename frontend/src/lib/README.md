# lib/

Small shared helpers used across pages and components — formatting rupees,
formatting a countdown, and so on.

Pure functions only. Anything that calls the backend belongs in `api/`;
anything holding state belongs in `hooks/`.
